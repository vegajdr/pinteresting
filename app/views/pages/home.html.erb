<div class="jumbotron center">
	<h1 style="font-family: 'Lobster', serif;">Welcome!</h1>
	<% if user_signed_in? %>
		Enjoy!
	<% else %>
	<p><%= link_to "Sign Up", new_user_registration_path, class: "btn btn-warning" %></p>
	<div><%= link_to "Login", new_user_session_path%></div>
	
	<% end %>
</div>